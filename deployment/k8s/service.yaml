apiVersion: v1
kind: Service
metadata:
  name: gan-cyber-range-service
  namespace: cyber-range
  labels:
    app: gan-cyber-range
spec:
  selector:
    app: gan-cyber-range
  ports:
    - 
port: 80
      targetPort: 8080
      name: http
    - 
port: 9090
      targetPort: 9090
      name: metrics
  type: ClusterIP