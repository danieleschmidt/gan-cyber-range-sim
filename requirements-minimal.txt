# Minimal core dependencies for basic functionality
# These are the absolute minimum needed for the system to work

# Core Python utilities
click>=8.1.0
rich>=13.6.0

# For simple async operations and data handling
asyncio-compat>=1.0.0 ; python_version < '3.7'

# Optional: For JSON schema validation (can be removed if needed)
jsonschema>=4.0.0

# Development and testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0